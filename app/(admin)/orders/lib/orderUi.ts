import { buttons, forms, glass, typography } from "@/theme";

export const glassPanel = glass.panel;
export const glassPanelTight = glass.card;

export const glassButton = buttons.secondary;
export const primaryButton = buttons.primary;

export const glassInput = forms.input;
export const glassSelect = forms.select;

export const labelText = typography.label;
export const smallMutedText = typography.caption;
