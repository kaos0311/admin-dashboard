/**
 * Shared test setup for the admin-dashboard test suite.
 *
 * This file runs before every test file, configuring Vitest globals
 * and importing shared matchers or mocks as needed.
 */

// Vitest globals are enabled in vitest.config.ts (globals: true).
// No additional setup is required for pure unit tests.
// Add Firebase mocks or Prisma mocks here when testing integration modules.
